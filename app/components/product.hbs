<LinkTo @route="item" @model={{@product.id}} class="product">
  <Product::Image @src={{this.productImage}} @isDetails={{@isDetails}} @zoom={{this.zoom}}
    @toggleZoom={{this.toggleZoom}} />
  <Product::Details @name={{@product.name}} @price={{@product.price}} />
</LinkTo>
